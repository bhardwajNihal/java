package StacksAndQueues.Queues.Questions;

public class Reverse {
 

// simple reverse of entire queue
    // simple
    // push to the stack
    // again add to the queuee


// reverse k elements of the queue
    // just empty the k elements to a stack
    // pop and add the n-k ele to another stack
    /// add the ele from the 1st stack to the queue,
    /// finally the the remaining ele from the 2nd stack to the queue
        // 2nd stack is required to maintain the order of the remaining elements
        

}
